{
  "name": "mbrd",
  "private": true,
  "license": "GPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "description": "Dev-only tooling. The app itself has no runtime dependency, and development needs no build step - the browser loads web/ directly and serve.py serves the modules. `npm test` needs no install; the three devDependencies are for the optional lint and typecheck runs and for `npm run build`, which bundles web/assets/app.js for release.",
  "scripts": {
    "start": "python tools/serve.py",
    "test": "node --test \"tests/**/*.test.js\"",
    "lint": "oxlint web tests tools",
    "typecheck": "tsc -p jsconfig.json",
    "build": "esbuild web/assets/js/main.js --bundle --minify --sourcemap --format=esm --target=es2022 --outfile=web/assets/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "oxlint": "^1.76.0",
    "typescript": "^5.7.0"
  }
}
